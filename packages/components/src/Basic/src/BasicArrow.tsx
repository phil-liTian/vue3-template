import { defineComponent } from "vue";
import { useDesign } from "../../hooks/useDesign";


export default defineComponent({
  name: 'PBasicArrow',
  setup(props) {
    return () => <div>1231</div>
  }
})